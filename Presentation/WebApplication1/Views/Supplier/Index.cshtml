@using Inventra.WebUI.Dtos.SupplierDtos
@model List<ResultSupplierDto>

@{
    ViewData["Title"] = "Tedarikçiler";
}

<div class="page-header">

    <div>

        <h2>
            Tedarikçiler
        </h2>

        <p class="text-muted mb-0">

            Toplam <strong>@Model.Count</strong> tedarikçi bulunmaktadır.

        </p>

    </div>

    <a asp-action="Create"
       class="btn btn-primary">

        <i class="bi bi-plus-lg me-2"></i>

        Yeni Tedarikçi

    </a>

</div>

<div class="content-card">

    @if (Model.Any())
    {
        <div class="table-responsive">

            <table class="table table-hover align-middle inventory-table">

                <thead>

                    <tr>

                        <th>Firma Adı</th>

                        <th>Telefon</th>

                        <th>E-Posta</th>

                        <th class="text-end" style="width:360px">

                            İşlemler

                        </th>

                    </tr>

                </thead>

                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>

                            <td class="fw-semibold">

                                @item.Name

                            </td>

                            <td>

                                @item.Phone

                            </td>

                            <td>

                                @item.Email

                            </td>

                            <td class="text-end">

                                <div class="d-inline-flex gap-2">

                                    <a asp-action="Update"
                                       asp-route-id="@item.Id"
                                       class="btn btn-outline-primary btn-sm">

                                        <i class="bi bi-pencil-square me-1"></i>

                                        Düzenle

                                    </a>

                                    <a asp-action="Delete"
                                       asp-route-id="@item.Id"
                                       class="btn btn-outline-danger btn-sm">

                                        <i class="bi bi-trash me-1"></i>

                                        Sil

                                    </a>

                                    <a asp-action="AiAnalysis"
                                       asp-route-id="@item.Id"
                                       class="btn btn-dark btn-sm">

                                        <i class="bi bi-robot me-1"></i>

                                        AI Analizi

                                    </a>

                                </div>

                            </td>

                        </tr>
                    }

                </tbody>

            </table>

        </div>
    }
    else
    {
        <div class="text-center py-5">

            <i class="bi bi-truck display-5 text-primary"></i>

            <h4 class="mt-3">

                Henüz tedarikçi bulunmuyor

            </h4>

            <p class="text-muted">

                İlk tedarikçinizi ekleyerek başlayabilirsiniz.

            </p>

            <a asp-action="Create"
               class="btn btn-primary mt-2">

                <i class="bi bi-plus-lg me-2"></i>

                İlk Tedarikçiyi Oluştur

            </a>

        </div>
    }

</div>